let assert = require('assert');
const { exec } = require('child_process');


describe('models/auths.model.js', async function () {

  // start the mongo server
  // exec('mongod');
  // await exec('npm start');

  describe('#init()', function () {

    it('should return an instance of Model', async function () {
      // let resp = await init();
      // assert(typeof resp === Model, `typeof resp = ${typeof resp}`);
      return assert(true);
    })
  })


})